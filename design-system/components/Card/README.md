A bone tile with a hairline border and the guide's 6px radius, for services, process steps and FAQ answers. Provide `title`, optional `eyebrow` (uppercase mono label), `body` and `items` (short deliverables, each gets a timber dot). Set `raised` for the white variant inside a bone section. Cards never carry a shadow or a coloured left border; weight comes from the forest section behind them.
