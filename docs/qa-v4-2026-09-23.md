# QA responsivo & cross-browser v4 — 2026-09-23 (commit 6cf6c52)

Engine: Chromium apenas (sandbox sem WebKit/Firefox); iPhone 13, Pixel 5, iPad e 844×390 por emulação. **Safari iOS real ainda não coberto** — recomendação: BrowserStack ou dispositivo físico antes do sign-off definitivo.

45 asserções automatizadas, todas PASS: sem overflow horizontal em 320/390/768/1000/1280/1440; hero cinza no load e cor após o 1º ridgeline (one-shot); 3 ridgelines acesos e rail em 1 após scroll incremental; 13 reveals entram uma vez e não somem; filtro da galeria com `aria-pressed` único, FLIP instantâneo em reduced-motion; tiles focáveis abrem em nova aba (`rel=noopener`); drawer abre/fecha por Escape e scrim, foco volta ao burger; FAQ por Enter e Espaço; validação e seletor de DDI do formulário; barra CTA aparece após o hero e some no contato; reduced-motion tudo em estado final; sem JS tudo visível.

Frame timing (3 runs × 6 viewports): 0 frames >32 ms exceto 1 frame de 33 ms em 1280×800 (~1 em 10 runs), atribuído à escrita de `--fill` do rail dentro do callback do IntersectionObserver. **Corrigido:** escrita adiada para um `requestAnimationFrame` por lote.
